---
type: lesson
title: About Client-only code
slug: intro-client-only
---

# Client-side JavaScript in Next.js

It's ironical that browser-only JavaScript is so hard to figure in Next.js, a React framework!
But that's the price to pay for server-side rendering.

Let's discover some patterns to never hit the dreaded `window not defined` error.

import BetaBanner from "@/components/BetaBanner.mdx"

<BetaBanner />